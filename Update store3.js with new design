export default function Store3() {
  const products = [
    { name: 'Mayo', price: 25, image: '/images/mayo.jpg' },
    { name: 'Tcha Tcha', price: 15, image: '/images/tchatcha.jpg' },
    { name: 'Braslè', price: 5, image: '/images/bracelet.jpg' },
    { name: 'Godé pou Chalè', price: 10, image: '/images/gode.jpg' },
    { name: 'Kepi', price: 15, image: '/images/kepi.jpg' },
  ];

  return (
    <div style={{ padding: '20px', fontFamily: 'Arial, sans-serif' }}>
      <h1 style={{ textAlign: 'center', color: '#E67E22' }}>
        🛍️ BSS Store – Koulè Jon Abriko & Nwa
      </h1>
      <div
        style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
          gap: '20px',
          marginTop: '30px',
        }}
      >
        {products.map((product, index) => (
          <div
            key={index}
            style={{
              border: '1px solid #ddd',
              borderRadius: '10px',
              overflow: 'hidden',
              textAlign: 'center',
              background: '#fff',
              boxShadow: '0px 4px 8px rgba(0,0,0,0.1)',
            }}
          >
            <img
              src={product.image}
              alt={product.name}
              style={{ width: '100%', height: '200px', objectFit: 'cover' }}
            />
            <h3 style={{ margin: '10px 0', color: '#E67E22' }}>{product.name}</h3>
            <p style={{ fontWeight: 'bold' }}>${product.price}</p>
          </div>
        ))}
      </div>
    </div>
  );
}
