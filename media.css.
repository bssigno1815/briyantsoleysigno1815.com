/* ====== BSS 1815 – style.css ====== */
:root{
  --bg:#0f0f10;          /* nwa */
  --fg:#f5f5f7;          /* blan mou */
  --muted:#9aa0a5;
  --apricot:#FFB347;     /* jòn abrikot */
  --apricot-2:#ff9c28;
  --card:#141414;
  --stroke:#26272a;
}

*{box-sizing:border-box}
html,body{margin:0}
body{
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background: radial-gradient(1200px 800px at 10% 0%, #111 0%, var(--bg) 60%) no-repeat var(--bg);
  color: var(--fg);
  line-height: 1.5;
}

/* Layout */
.container{max-width: 1000px; padding: 24px 18px; margin: 0 auto;}
.header-wrap{display:flex; align-items:center; justify-content:space-between; gap:16px}
.brand{display:flex; align-items:center; gap:12px}
.brand .dot{width:12px; height:12px; border-radius:50%; background:var(--apricot); box-shadow:0 0 18px var(--apricot)}
.brand h1{font-size: clamp(18px, 3vw, 24px); margin:0}
nav a{
  display:inline-block; margin:0 8px; padding:10px 14px;
  color:var(--fg); text-decoration:none; border:1px solid var(--stroke);
  border-radius:12px; transition: transform .2s ease, background .2s, border-color .2s;
  background:rgba(255,255,255,.02);
}
nav a:hover{transform: translateY(-1px); background:#111; border-color:var(--apricot); color:#fff}
nav a.active{background:var(--apricot); color:#000; border-color:var(--apricot)}

.hero{margin-top:20px; border:1px solid var(--stroke); border-radius:16px; background: linear-gradient(90deg,#141414,#181818); padding:28px}
.hero h2{margin:0 0 8px 0; font-size: clamp(22px, 4vw, 28px)}
.hero p{color:var(--muted); margin:0}

.section{margin:28px 0}
.card{
  background:var(--card); border:1px solid var(--stroke);
  border-radius:16px; padding:20px; box-shadow:0 10px 24px rgba(0,0,0,.25);
}

/* Buttons */
.btn{
  display:inline-block; padding:10px 16px; border-radius:12px;
  border:1px solid var(--apricot); color:#000; background:var(--apricot);
  font-weight:700; text-decoration:none; transition:filter .2s, transform .2s;
}
.btn:hover{filter:brightness(1.05); transform:translateY(-1px)}
.btn.ghost{background:transparent; color:var(--fg); border-color:var(--stroke)}
.btn.ghost:hover{border-color:var(--apricot); color:#fff}

/* Gallery grid */
.grid{display:grid; gap:14px; grid-template-columns: repeat(auto-fill, minmax(180px,1fr))}
.grid img{width:100%; height:180px; object-fit:cover; border-radius:14px; border:1px solid var(--stroke); transition:transform .2s}
.grid img:hover{transform:scale(1.02)}

/* Media embeds */
.embed{position:relative; width:100%; padding-bottom:56.25%; border-radius:16px; overflow:hidden; border:1px solid var(--stroke); background:#000}
.embed iframe, .embed video{position:absolute; inset:0; width:100%; height:100%}

/* Forms */
form{display:grid; gap:12px}
input, textarea{
  width:100%; background:#0f0f10; color:var(--fg);
  border:1px solid var(--stroke); border-radius:12px; padding:12px;
}
input::placeholder, textarea::placeholder{color:#7d848a}
button[type="submit"]{all:unset}
button[type="submit"].btn{cursor:pointer}

/* Animations */
.fade-in{animation:fade .5s ease both}
@keyframes fade{from{opacity:0; transform:translateY(6px)} to{opacity:1; transform:none}}

footer{margin:36px 0 10px 0; color:var(--muted); text-align:center; font-size:14px}
