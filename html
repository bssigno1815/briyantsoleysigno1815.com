<!doctype html>
<html lang="ht">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Briyant Solèy Signo 1815 — Ofisyèl</title>
  <meta name="description" content="Briyant Solèy Signo 1815 — Toujou brile, toujou kléré. Everything Black & Orange." />
  <style>
    :root{
      --apricot:#F49B1A; /* jòn abriko */
      --black:#0b0b0b;   /* nwa */
      --white:#ffffff;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0}
    body{
      font-family: Arial, system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
      color:var(--white);
      background:linear-gradient(180deg,var(--apricot) 0%,#c17510 25%,#463414 55%,var(--black) 100%);
    }

    /* Top nav */
    .nav{
      position:sticky; top:0; z-index:10;
      backdrop-filter: blur(6px);
      background:rgba(0,0,0,.35);
      border-bottom:1px solid rgba(255,255,255,.08);
    }
    .nav-inner{
      max-width:1100px; margin:0 auto; padding:10px 16px;
      display:flex; align-items:center; gap:14px; justify-content:space-between;
    }
    .brand{display:flex; align-items:center; gap:10px; font-weight:700; letter-spacing:.3px}
    .brand img{width:36px;height:36px;object-fit:contain}
    .links{display:flex; gap:10px; flex-wrap:wrap}
    .links a{
      color:var(--white); text-decoration:none; border:1px solid #333;
      padding:8px 12px; border-radius:999px; font-size:14px
    }
    .links a:hover{border-color:var(--apricot); color:var(--apricot)}

    /* Sections */
    section{padding:56px 18px}
    .wrap{max-width:1100px; margin:0 auto}
    .hero{
      display:grid; gap:18px; text-align:center; padding-top:42px;
    }
    .hero .logo{width:160px;height:160px;object-fit:contain;margin:0 auto}
    h1{margin:0; font-size:40px; letter-spacing:.5px}
    .tag{opacity:.95; margin-top:6px; font-size:18px}

    /* Buttons */
    .btns{display:flex; gap:12px; justify-content:center; flex-wrap:wrap; margin-top:16px}
    a.btn{
      background:transparent; color:var(--white); border:1px solid #333;
      padding:10px 16px; border-radius:999px; text-decoration:none; font-weight:600
    }
    a.btn:hover{border-color:var(--apricot); color:var(--apricot)}

    /* About */
    .about p{line-height:1.7; margin:0 0 12px}

    /* Contact */
    .grid{display:grid; gap:14px; grid-template-columns:repeat(auto-fit,minmax(260px,1fr))}
    .pill{
      background:#111; border:1px solid #222; border-radius:14px; padding:14px;
      display:flex; align-items:center; justify-content:space-between; gap:10px
    }
    .pill a{color:var(--white); text-decoration:none; word-break:break-all}
    .copy{cursor:pointer; border:1px solid #333; border-radius:999px; padding:6px 10px}
    .copy:hover{border-color:var(--apricot); color:var(--apricot)}

    form{margin-top:8px; display:grid; gap:10px}
    input,textarea{
      border:1px solid #333; background:#0f0f0f; color:#fff; border-radius:10px; padding:10px
    }
    button.submit{
      background:transparent; color:#fff; border:1px solid #333; border-radius:999px;
      padding:10px 14px; cursor:pointer; font-weight:600
    }
    button.submit:hover{border-color:var(--apricot); color:var(--apricot)}

    footer{
      text-align:center; padding:26px 16px; opacity:.8; font-size:13px;
      border-top:1px solid rgba(255,255,255,.08)
    }

    /* Toast */
    .toast{
      position:fixed; left:50%; bottom:24px; transform:translateX(-50%) translateY(20px);
      background:var(--apricot); color:#000; padding:10px 14px; border-radius:999px;
      box-shadow:0 8px 20px rgba(0,0,0,.4); opacity:0; pointer-events:none;
      transition:opacity .2s, transform .2s;
    }
    .toast.show{opacity:1; transform:translateX(-50%) translateY(0)}
  </style>
</head>
<body>

  <!-- NAV -->
  <header class="nav">
    <div class="nav-inner">
      <div class="brand">
        <img src="assets/logo.png" alt="Logo BSS"
             onerror="this.style.display='none'">
        <span>BSS 1815</span>
      </div>
      <nav class="links">
        <a href="#home">Akèy</a>
        <a href="#about">Kiyès nou ye</a>
        <a href="#contact">Kontakte</a>
      </nav>
    </div>
  </header>

  <!-- HERO -->
  <section id="home">
    <div class="wrap hero">
      <img class="logo" src="assets/logo.png" alt="Logo BSS"
           onerror="this.style.display='none'">
      <h1>Briyant Solèy Signo 1815</h1>
      <p class="tag"><strong>Toujou brile, toujou kléré — Everything Black & Orange.</strong></p>
      <div class="btns">
        <a class="btn" href="#about">Kiyès nou ye</a>
        <a class="btn" href="#contact">Kontakte nou</a>
      </div>
    </div>
  </section>

  <!-- ABOUT -->
  <section id="about">
    <div class="wrap about">
      <h2>Kiyès nou ye</h2>
      <p>
        BSS 1815 se yon mouvman kiltirèl ak kominotè. Koulè ofisyèl: 
        <em>jòn abriko & nwa</em>. Nou pote enèji, respè, ak inite nan chak aktivite.
      </p>
      <p>
        Misyon nou: klere kote nou ye, sipòte jèn yo, epi fè kominote a mache pi byen.
      </p>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact">
    <div class="wrap">
      <h2>Kontakte nou</h2>

      <!-- Vizib, klike & Kopye -->
      <div class="grid" aria-label="Imèl BSS">
        <div class="pill">
          <a href="mailto:briyantsoleysigno1815@gmail.com">
            briyantsoleysigno1815@gmail.com
          </a>
          <button class="copy" data-email="briyantsoleysigno1815@gmail.com">Copy</button>
        </div>

        <div class="pill">
          <a href="mailto:briyantsigno@gmail.com">briyantsigno@gmail.com</a>
          <button class="copy" data-email="briyantsigno@gmail.com">Copy</button>
        </div>
      </div>

      <!-- Ti fòm san backend: ouvri app imèl la -->
      <form action="mailto:briyantsoleysigno1815@gmail.com" method="post" enctype="text/plain">
        <input type="text" name="non" placeholder="Non ou" required>
        <input type="email" name="imel" placeholder="Imèl ou" required>
        <textarea name="mesaj" rows="5" placeholder="Mesaj ou"></textarea>
        <button class="submit" type="submit">Voye mesaj</button>
      </form>
    </div>
  </section>

  <footer>© 1815—2025 BSS • Jòn abriko & nwa pou tout tan.</footer>

  <div id="toast" class="toast">Kopye!</div>
  <script>
    // Copy buttons
    const toast = document.getElementById('toast');
    document.addEventListener('click', async (e)=>{
      const btn = e.target.closest('.copy'); if(!btn) return;
      const email = btn.dataset.email;
      try { await navigator.clipboard.writeText(email); }
      catch(e){
        const ta=document.createElement('textarea'); ta.value=email; document.body.appendChild(ta);
        ta.select(); document.execCommand('copy'); ta.remove();
      }
      toast.textContent = 'Kopye: ' + email;
      toast.classList.add('show'); setTimeout(()=>toast.classList.remove('show'), 1200);
    });
  </script>
</body>
</html>
