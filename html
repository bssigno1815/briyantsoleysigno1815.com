<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>BRIYANT SOLÈY SIGNO 1815</title>
    <style>
      :root { --orange:#f97316; --black:#000; --white:#fff; }
      html,body { height:100%; margin:0; }
      body { display:grid; place-items:center; background:var(--black); color:var(--white); font-family:system-ui, Arial, sans-serif; }
      .wrap { text-align:center; padding:2rem; }
      h1 { color:var(--orange); font-size:clamp(28px,5vw,56px); margin:0 0 .5rem; letter-spacing:.04em; }
      p { opacity:.9; margin:.25rem 0; }
      .btn { display:inline-block; margin-top:1rem; padding:.7rem 1rem; border:2px solid var(--orange); color:var(--black);
             background:var(--orange); border-radius:.75rem; text-decoration:none; font-weight:700; }
      .btn:hover { background:transparent; color:var(--orange); }
    </style>
  </head>
  <body>
    <div class="wrap">
      <h1>BRIYANT SOLÈY SIGNO 1815</h1>
      <p>🍊⚫ Orange & Black only. ⚫🍊</p>
      <p>Site ap monte etap pa etap…</p>
      <a class="btn" href="/">Antre</a>
    </div>
  </body>
</html>
