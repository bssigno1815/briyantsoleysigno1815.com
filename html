<!doctype html>
<html lang="ht">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Briyant Solèy Signo 1815 — Live ✅</title>

  <!-- Si ou gen /css/styles.css deja, dekomante liy sa a -->
  <!-- <link rel="stylesheet" href="/css/styles.css" /> -->

  <style>
    :root{
      --bg:#0f0f10;        /* nwa fon */
      --card:#18181b;      /* gri fonse pou kat yo */
      --fg:#f5f5f7;        /* tèks limyè */
      --muted:#9aa0a5;     /* gri tèks segondè */
      --accent:#ff8c42;    /* jòn-abriko */
      --accent-2:#2f81f7;  /* ble ti detay (opsyonèl) */
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      color:var(--fg);
      background: radial-gradient(1200px 600px at 20% -10%, #111 0%, transparent 60%),
                  radial-gradient(1000px 500px at 120% 10%, #111 0%, transparent 60%),
                  var(--bg);
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      line-height:1.5;
    }
    header{
      padding:22px 16px;
      border-bottom:1px solid #222;
      background:linear-gradient(90deg, var(--accent), #ffd0a6 60%, transparent 60%);
      background-size: 180px 100%;
      background-repeat:no-repeat;
      color:#111;
      font-weight:800;
    }
    .wrap{max-width:920px;margin:0 auto;padding:22px 16px}
    nav{display:flex;gap:12px;flex-wrap:wrap;margin:6px 0 16px}
    nav a{
      display:inline-block;text-decoration:none;font-weight:700;
      padding:10px 14px;border:1px solid #333;border-radius:12px;
      background:var(--card);color:#fff;transition:.2s;
    }
    nav a:hover{border-color:var(--accent); box-shadow:0 0 0 2px #000, 0 0 0 4px var(--accent)}
    .card{
      background:var(--card);
      border:1px solid #26272a;border-radius:14px;padding:20px;
      box-shadow: 0 10px 30px rgba(0,0,0,.25);
    }
    .muted{color:var(--muted)}
    footer{margin:36px 0;color:var(--muted);font-size:14px}
  </style>
</head>
<body>
  <header class="wrap">Briyant Solèy Signo 1815 — Live ✅</header>

  <div class="wrap">
    <nav>
      <a href="/music/">🎵 Music</a>
      <a href="/contact/">📬 Contact</a>
    </nav>

    <section class="card">
      <h1>Byenvini!</h1>
      <p class="muted">
        Sa a se paj prensipal la. Klike sou “Music” pou w wè kontni mizik yo,
        oswa “Contact” pou kontakte nou.
      </p>
    </section>

    <footer>Dènye mizajou: <span id="ts"></span></footer>
  </div>

  <script>
    // montre dat an kreyòl (HT)
    const el = document.getElementById('ts');
    if (el) el.textContent = new Date().toLocaleString('ht-HT', {
      year:"numeric", month:"2-digit", day:"2-digit",
      hour:"2-digit", minute:"2-digit", second:"2-digit"
    });
  </script>
</body>
</html>
